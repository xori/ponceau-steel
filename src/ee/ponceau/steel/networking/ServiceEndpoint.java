package ee.ponceau.steel.networking;

/**
 *  An `API Endpoint` for getting information from the client about things that
 * it creates / controls. An example of which would the players that the client
 * is in charge of / stats / or plugins.
 * 
 * @author Evan
 */
public interface ServiceEndpoint {
  
}
