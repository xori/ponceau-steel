package ee.ponceau.steel;

import ee.ponceau.steel.definitions.Entity;
import java.awt.geom.Ellipse2D;
import java.awt.geom.Rectangle2D;

/**
 * Not sure where this should be.
 * @author Evan
 */
public class Physics {
//  public boolean collideCircles(Ellipse2D c1, Ellipse2D c2){
//    
//    return false;
//  }
//  
//  public boolean collideBoxes(Entity r1, Entity r2) {
//    return this.position.x > e.position.x + e.dimension.x ? false :
//            this.position.y > e.position.y + e.dimension.y ? false :
//            this.position.x + this.dimension.x < e.position.x ? false :
//            this.position.y + this.dimension.y < e.position.y ? false : true;
//  }
}
